services:
  - type: web
    name: show-schedule-builder
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web_scheduler.py
    autoDeploy: true
